import { createStyles, makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() =>
  createStyles({
    image: {
      width: "100%",
      height: "auto",
      borderRadius: 2,
    },
  })
);
