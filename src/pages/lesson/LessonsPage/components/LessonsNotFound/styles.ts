import { createStyles, makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() =>
  createStyles({
    container: {
      textAlign: "center",
      width: "85%",
      borderRadius: 2,
    },
  })
);
